interface products {
    id:number
    title:string
    description:string
    image:string
}
/* models/interface.ts */

export interface Municipality {
    id: string;
    district_name: string;
    name: string;
}